document.addEventListener("DOMContentLoaded",()=>{const menu=document.querySelector(".menu-toggle"),nav=document.querySelector(".main-nav");if(menu){menu.addEventListener("click",()=>{const open=nav.classList.toggle("open");menu.setAttribute("aria-expanded",open)})}nav?.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{nav.classList.remove("open");menu?.setAttribute("aria-expanded","false")}));const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("revealed");observer.unobserve(e.target)}})},{threshold:.12});document.querySelectorAll(".reveal").forEach(el=>observer.observe(el));const prev=document.querySelector(".prev"),next=document.querySelector(".next"),track=document.querySelector(".testimonial-track");let x=0;const slide=d=>{if(!track)return;const card=track.querySelector(".testimonial");if(!card)return;x+=d;const max=Math.max(0,track.children.length-1);x=Math.max(0,Math.min(max,x));if(window.innerWidth<=900){track.style.transform=`translateX(-${x*100}%)`;track.style.display="flex";track.style.transition="transform .35s ease";track.children[0].style.minWidth="100%";[...track.children].forEach(c=>c.style.minWidth="100%")} };prev?.addEventListener("click",()=>slide(-1));next?.addEventListener("click",()=>slide(1));window.addEventListener("resize",()=>{if(track&&window.innerWidth>900){track.style.transform="";track.style.display="grid";[...track.children].forEach(c=>c.style.minWidth="")}});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener("click",e=>{const id=a.getAttribute("href");if(id&&id!=="#"){const target=document.querySelector(id);if(target){e.preventDefault();target.scrollIntoView({behavior:"smooth",block:"start"})}}}));});